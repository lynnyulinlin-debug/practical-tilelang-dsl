# 第 1 章导言：Kernel 与 DSL：AI 编译器的动机

## English Title

From Kernel to DSL

## 章节定位

这一章先解决一个最常见的入门困惑：代码明明已经跑通了，为什么一换 shape、换算子、换后端，原来的写法就又开始返工。  
它不是先讲术语表，而是先让你看到一个很现实的工程问题：kernel 不是只要“能写出来”就结束了，后面还有维护、迁移和调优。

也正因为如此，TileLang 不是“另一个 kernel 语法”，而是把结构、调度和后端落地放进同一条可管理链路里的 DSL。它关心的不是多写一层包装，而是让后面的 shape、layout、tile 和后端选择能在更合适的层次上被处理。

而这条链路之所以能成立，靠的就是编译器。它不是简单把代码“翻译一下”，而是把 TileLang 里显式写出的结构、约束和硬件意图继续往下传，最后落到真正能在目标平台上运行的代码上。更具体一点，编译器擅长自动完成那些规则清楚、合法性强的落地优化；而算子优化往往要先靠人判断瓶颈在哪，再决定 tile、layout、fusion 这些方向。这个顺序也会贯穿后面的章节：先有编译链路，后看访存是否合并、布局是否合理，再往下看同步和通信边界怎么影响整体性能。第 3 章会把这座桥讲得更具体，第 4、5、8、10 章则会继续看到这座桥如何把不同层次连起来。在这套教程里，TVM 主要扮演的是这条链路可运行、可验证、可对照的编译和运行底座，所以你会在第 3 章先接触到环境与最小闭环，在后面的章节再逐步看到它如何承接不同后端。

所以这一章的任务很简单：先让读者知道为什么要继续学，再把 kernel、DSL、compiler 这几个词放到同一条路径里。这样，后面第 2 章的硬件、第 3 章的入门、第 4 章的分块、第 8 章的多后端，就不再是散开的知识点，而是同一条链路上的下一步。

也正因为这个原因，第 1 章先讲编译器不是为了把编译器本身讲成主角，而是先把 DSL 为什么需要一座桥说清楚：TileLang 不是只靠“写 kernel”就能成立的 DSL，它真正关心的是你把结构、约束和硬件意图写出来之后，怎样通过编译链路把这些东西稳定传到后端。先把这件事讲顺，后面的硬件层次、调度优化、多后端迁移和项目闭环才会自然接上，而不是变成彼此孤立的知识点。

## 本章最小深度要求

读完这一章，先能回答这三个最基本的问题：

- 从一次“能跑”的 kernel 讲起，说明它为什么很快会变成“难维护”的 kernel
- 为什么 AI kernel 的难点不只是计算，还包括表达、优化和迁移
- 为什么 TileLang 选择把 kernel、DSL 和编译器放在同一条链路里

## 本章目标

- 讲清 `kernel`、`DSL`、`compiler` 的边界
- 说明传统编译器和 AI 编译器分别关注什么
- 说明 AI 场景为什么放大抽象层的价值
- 给出 TileLang 在整套工具链里的位置
- 为后续硬件、调度和算子章节统一术语

更重要的是，这一章要把“为什么要分层”讲成一个直觉，而不是一组定义。先知道问题为什么会出现，再去看分层怎么解决它，学习负担会小很多。

## 本章判断标准

如果这章写得够清楚，读者不仅知道“DSL 是什么”，还应该知道：

- 什么时候继续手写 kernel 更合适
- 什么时候应该把问题上提到 DSL 层
- 什么时候更像传统编译器在处理的问题，什么时候更像 AI 编译器在处理的问题
- 编译器在哪些地方帮忙，在哪些地方不会替你兜底
- TileLang 和 CUDA、Triton、TVM 的关系分别是什么

## 内容块安排

### 1.1 问题背景
- 先看一个“能跑但不想再改”的 kernel 场景
- 接着看为什么同类变化会反复碰到同一批实现细节
- 最后说明为什么 AI 场景会把这个问题放大

### 1.2 Kernel 与 DSL
- 在 1.1 的返工场景上，继续看为什么变化会把实现推向更高一层
- 再把 kernel 和 DSL 的最小区别说清楚
- 只保留一个简单判断：这一版怎么写，和这一类怎么写得更稳，不是一回事

### 1.3 AI 编译器的角色
- 先把编译器当成 DSL 和可执行结果之间的桥
- 只看它为什么需要存在，不急着看内部怎么展开
- 再说明为什么 TileLang 需要这座桥

### 1.4 TileLang 的位置
- 继续沿着前面的场景，看 TileLang 为什么出现在这条链上
- 重点说明它不是“另一个 kernel 语法”，而是一个工程组织方式
- 只在必要时提到它和 CUDA、Triton、TVM 的关系

### 1.5 本章总结
- 用一段简短结论收口
- 告诉读者现在已经知道什么
- 顺手把下一章的硬件模型引出来

## 图和公式占位

- `Kernel / DSL / Compiler / Backend` 关系图
- 问题对比表
- AI 计算动机示意图
- 一张“直接手写 kernel vs DSL + 编译器”的维护成本对比图
- 一张“表达层 / 编译层 / 执行层”三层结构图

这一章的抽象层次还是偏高，所以图表要尽量少而准，先帮读者把 kernel、DSL 和编译器的关系一次看清。分工最好是：

- 关系图负责说明三层如何分工
- 对比表负责说明为什么分层有价值
- 动机示意图负责说明为什么问题会从 kernel 走向 DSL
- 三层结构图负责给后面的硬件、编译和优化章节固定坐标

如果只补其中一张，读者可能知道“有分层”，但还不一定知道每一层分别解决什么问题。所以这一章的图表不是装饰，而是把整本书的判断框架先画出来，但它们必须服务理解，不应该抢走叙述本身的作用。

## 与后续章节的关系

- 第 2 章会把抽象落到硬件层次
- 第 3 章会把抽象落到第一个可运行 kernel，也会顺手把编译链路讲得更具体一点
- 第 4、5 章会继续把这些概念用于优化判断

如果你是第一次读这套教程，最顺的方式是先把第 1 章看完，然后接第 3 章建立最小闭环；第 2 章可以在第 3 章之后补读，用来把分层概念落到硬件层次。接下来再顺着第 4、5、8、9、10 章往下走，就能把编译、性能、迁移和项目闭环连成一条线。

## 写作提醒

这一章最重要的不是术语列表，而是把“为什么需要 DSL”和“为什么需要编译器”讲成一条连贯的工程链。  
如果读者在这里还只记住了定义，没有记住问题从哪里来、为什么必须这样分层，后面第 2 到第 5 章就会变成零散知识点。

换句话说，这一章的目标不是“知道三种名词”，而是先建立一套顺序：先看到返工问题，再看到抽象层价值，最后再看到编译链路为什么必要。对初学者来说，这个顺序比完整定义更重要，先有方向感，后面再补术语，会轻松很多。

## 教学入口

- 对应 notebook：`examples/01_why_dsl.ipynb`
- 对应判断脚本：`examples/01_why_dsl.py`
