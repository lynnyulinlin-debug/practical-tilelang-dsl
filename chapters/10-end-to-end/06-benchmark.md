# 10.6 端到端 benchmark

## 要解决什么问题

端到端项目的最终判断标准不是某个算子单点多快，而是整体链路有没有真实收益。

这一节要把“项目有没有价值”落到统一指标上，而不是只看局部优化感觉。

这里的重点不是把指标堆得越多越好，而是把指标分层后各自看懂。
如果单算子、子图和端到端三层混在一起，读者很容易误把局部收益当成整体收益。

## 应该记录的指标

- token/s
- latency
- throughput
- 峰值显存
- 平均显存
- 迁移前后加速比
- 小规模趋势扫描结果

## 记录模板

- 直接复用 [QUICKREF.md](../../QUICKREF.md) 里的 benchmark 模板
- 端到端场景只需要在其基础上补充模型名、子图名和系统级指标

模板里最好明确三件事：

1. 测试对象是什么
2. 统计口径是什么
3. 这次数据能说明哪一层的结论

只有这三件事都清楚，后面的表和图才不会变成孤立数字。

## 这里应该放的记录样例

> 这里可以先给出一个端到端项目的最小记录样例，帮助后续落表。

```python
from examples.benchmark_record_template import set_benchmark_record

record = set_benchmark_record(
    scenario="chapter 10 end-to-end migration",
    operator="subgraph",
    platform="multi-backend",
    target="cuda",
    dtype="float16",
    shape="subgraph specific",
    baseline="original implementation",
    optimization="iterative migration + backend tuning",
)
```

如果启用了小规模扫描，记录里还应该额外补上：

- `sweep_enabled`
- 不同 `seq_len` 下的 latency
- 不同 `seq_len` 下的 speedup

## 对比维度

- 单算子前后
- 子图前后
- 端到端前后
- 单后端与多后端

这四类对比不要混着写。
单算子前后主要看局部结构是否成立，子图前后主要看中间张量是否减少，端到端前后主要看业务链路是否受益，多后端对比主要看迁移边界是否稳定。

## 一个更稳的记录顺序

1. 先记录单算子指标
2. 再记录子图级指标
3. 然后记录端到端指标
4. 最后才做跨后端比较

如果项目里启用了 `sweep`，还应该把规模扫描插在单算子和端到端之间，作为趋势验证的一层。

这个顺序的目的，是先把局部事实钉住，再看整体结论是否成立。
如果反过来先写端到端结论，后面往往很难解释收益到底来自哪里。

## 当前写作口径

- 先统一指标，再统一口径
- 不要把局部 benchmark 当作最终结论
- 如果有趋势扫描，优先看趋势是否稳定，而不是只看一个 shape 的最优值
- 如果数据不完整，也要先把表格结构定下来

## 这里应该配的图

**图：端到端性能对比图**

> 这里要展示整体链路在迁移前后的吞吐、延迟和显存变化。

**图：单算子收益汇总图**

> 这里要说明哪些收益来自 GEMM，哪些来自 attention 或融合算子。

**图：显存占用变化图**

> 这里要展示中间张量消除和布局优化后显存峰值的变化。

这一节的图最好按层级分开补：单算子一张、子图一张、端到端一张，避免把不同层级的变化混在一张图里。

## 小结

端到端 benchmark 的价值在于把单点优化收益转化成工程收益。
如果没有整体指标，前面的所有优化都很难证明其业务价值。
如果有趋势扫描，读者还能知道这种收益是否只是某个 shape 的偶然结果。

## 这一节可以进一步追问的问题

- 单算子、子图和端到端指标分别说明什么
- 哪些指标应该固定口径
- 为什么跨后端比较一定要先统一统计方式

## 常见误区

- 误区 1：单点 benchmark 就足够说明项目价值
- 误区 2：不同层级的指标可以混着看
- 误区 3：只要性能提高，口径就不重要
- 误区 4：某个 shape 上的最优值可以直接推广到所有规模

端到端 benchmark 的意义，是把工程收益和单点收益区分开。
趋势扫描的意义，是把偶然的单点收益和相对稳定的规模收益区分开。

如果项目里只有一个结果点，没有趋势、没有对照、没有层级，那就只能说明“这次跑出来了”，还不能说明“这个迁移方法可复用”。

## 讲义提示

如果这节要落成表格，建议分成三层：

- 算子级
- 子图级
- 端到端级

这样读者会明白 benchmark 不是只记录一个数字，而是记录一条完整链路。
