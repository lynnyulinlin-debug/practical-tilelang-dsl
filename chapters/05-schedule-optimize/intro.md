# 第 5 章导言：调度优化与性能分析

## 章节定位

这一章解决“怎么把已经写对的 kernel 调得更快”。  
它是从结构化表达走向系统化调优的过渡章。

## 本章目标

- 选择合适的并行层级
- 用流水线和双缓冲隐藏延迟
- 用布局和 swizzle 改变访问模式
- 用 Roofline 和 profiler 形成诊断闭环
- 让读者知道“调优的顺序”和“调优的停止点”同样重要

## 本章判断标准

读完这一章，读者应该能判断：

- 先改并行层级，还是先改流水线
- 先改布局，还是先看算术强度
- 某个 kernel 是该继续调参，还是应该停下来验证当前结构已经足够好
- autotuning 应该在哪里介入，哪里不该盲搜

## 内容块安排

### 5.1 并行策略
- Grid / Block / Thread
- 并行层级选择

### 5.2 流水线与双缓冲
- 计算与加载重叠
- latency hiding

### 5.3 内存布局优化
- 行主序 / 列主序
- `T.annotate_layout`
- `swizzle`

### 5.4 Roofline 与性能分析
- 算术强度
- memory-bound / compute-bound
- profiler 入口

### 5.5 自动调优
- 搜索空间
- AutoTVM
- 参数评估

### 5.6 本章总结
- 给出一个调优闭环模板

## 图和公式占位

- 并行层级图
- 流水线时序图
- Roofline 图
- 调优空间图

这一章的关键是调优顺序和停止点，后续建议补一张调优流程图和一张 Roofline 图，帮助读者区分“该改什么”和“为什么该停”。

## 本章最小深度要求

这一章至少要让读者回答：

1. 为什么要先看并行层级，再看流水线，再看布局
2. 为什么 Roofline 不是结果本身，而是判断工具
3. 为什么调优不能只看单次 benchmark
4. 为什么要知道什么时候该停

如果只是记住几个优化名词，这章还不够。

## 教学入口

- 对应总览脚本：`examples/05_schedule_optimize_demo.py`
- 对应拆分脚本：`examples/05_roofline_analysis.py`、`examples/05_autotuning_demo.py`

## 与后续章节的关系

- 第 6、7 章会把这些方法具体用到算子项目里
- 第 8 章会把多后端适配加入工程要求
- 第 10 章会把这些分析方法用于端到端项目
