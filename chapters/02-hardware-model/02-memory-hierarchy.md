# 2.2 GPU 内存层次

## 要说明的问题

GPU 性能瓶颈通常不在“算得不够快”，而在“数据来得不够快”。

## 要点

- `Register` 最快，但容量最小，线程私有
- `Shared Memory` 允许 block 内复用
- `L2 Cache` 由整个 GPU 共享
- `HBM / Global Memory` 容量大，但延迟最高
- 内存越快，通常越小、越局部、越需要显式管理
- 同样的数据如果能在更高层被复用一次，整体性能通常就会改善

## 这里应该配的图

**图：Register / Shared / L2 / Global 的层次图**

> 这里要说明每一层的访问成本、容量和可见范围，帮助读者理解为什么 tile 和数据复用是必须的。

## 这里应该放的公式

**公式：访问延迟换算**

> 这里应把 ns 换算成 cycle，说明为什么 HBM 的等待会被大量线程并行掩盖，但不能被无限掩盖。

## 这里应该解释的现象

- 为什么对齐访问更快
- 为什么随机访问会浪费带宽
- 为什么 bank conflict 会让 shared memory 退化
- 为什么“把数据搬上来”并不自动等于“性能提升”

## 判断标准

读到这里，读者应该能做出下面几类判断：

- 哪些数据值得放进 shared memory
- 哪些数据只适合短暂驻留在 register
- 哪些访问模式会因为 stride 或随机访问而浪费带宽
- 哪些场景下，复用比单纯并行更重要

## 常见误区

- 误区 1：shared memory 越多越好
- 误区 2：只要显存够大，访问代价就不重要
- 误区 3：所有瓶颈都能靠增加线程解决

实际上，内存层次的关键不是“有多少层”，而是“数据是否落在合适的层上”。  
Tile 的作用，就是把数据尽量放到该放的层次里。
