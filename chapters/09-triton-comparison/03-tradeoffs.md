# 9.3 自动优化 vs 显式控制

## 要解决什么问题

这一节比较 TileLang 和 Triton 的核心差异，尤其是“谁控制什么”。

它不是在做阵营对立，而是在讲两条路线各自把复杂度放在哪里。

## 要讲什么

### 1. Triton 偏自动

- 更高层的抽象
- 编译器处理更多实现细节
- 写法更紧凑
- 更适合先把 kernel 跑起来，再慢慢看性能

### 2. TileLang 偏显式

- 开发者显式控制更多内存层次
- 调度和布局可见
- 更适合细粒度优化和跨硬件迁移
- 更适合把内存流、计算流和调度流拆开看

### 3. 差异带来的后果

- Triton 更快进入实验
- TileLang 更容易做结构化调优
- 两者的优势不在同一维度

### 4. 该怎么看这个差异

- 如果你要快速试算子，先看 Triton 的开发效率
- 如果你要跨平台迁移或压榨硬件，先看 TileLang 的控制边界
- 如果你要教学，TileLang 更容易把“哪里可控”讲清楚

### 4. 当前写作口径

- 先对比控制边界，再对比适用场景
- 不要把“自动优化”和“显式控制”写成价值判断
- 这节的目标是帮助读者建立选择标准

### 5. 一个更实用的判断

```text
need quick experiment?
  -> Triton
need cross-platform migration?
  -> TileLang
need explicit layout / schedule control?
  -> TileLang
need minimal code to verify a hot path?
  -> Triton
```

## 这里应该配的表

**表：TileLang vs Triton 的能力对比表**

> 这里要说明抽象层级、内存控制、硬件支持和优化方式的差别。

这张表应该直接服务于选型判断，后续优先补出“适用场景”和“控制边界”两列，避免只写优点不写代价。

## 这里应该配的图

**图：自动优化 vs 显式控制对比图**

> 这里要说明复杂度是如何在编译器和开发者之间分配的。

这张图最好放在表之后，用来把“差异”画成一条连续的控制边界。

## 小结

自动优化和显式控制不是对错关系，而是不同的工程策略。
读者真正需要的是根据目标平台、团队能力和迁移需求来选路线。

## 这一节可以进一步追问的问题

- 哪些复杂度被 Triton 处理掉了，哪些留给开发者
- 哪些复杂度被 TileLang 显式暴露了，为什么这有价值
- 为什么两条路线的复杂度分布不同

## 常见误区

- 误区 1：自动优化一定更好
- 误区 2：显式控制一定更强
- 误区 3：两条路线只是编码风格不同

这节真正要讲的，是复杂度放在谁手里、放在哪一层。

## 讲义提示

这一节最适合用“问题 -> 路线 -> 代价”的方式来讲：

- 问题是什么
- 哪条路线更快解决
- 代价是什么

这样读者不会把它看成抽象哲学，而会把它看成工程决策表。
